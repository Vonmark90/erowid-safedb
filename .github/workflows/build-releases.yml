name: Build and Release Standalone Apps

'on':
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release Tag (e.g. v2.5.0)'
        required: false
        default: 'v2.5.0'

permissions:
  contents: write

jobs:
  build-windows:
    name: Build Windows Standalone (.exe)
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Build Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build Windows Executable
        run: |
          pyinstaller --noconfirm --onefile --windowed --name "ErowidSafeDB" --add-data "data;data" --add-data "assets;assets" gui.py
          Compress-Archive -Path dist\ErowidSafeDB.exe -DestinationPath ErowidSafeDB-Windows-x64.zip

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-binary
          path: ErowidSafeDB-Windows-x64.zip

  build-linux:
    name: Build Linux Standalone Executable
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Build Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build Linux Binary
        run: |
          pyinstaller --noconfirm --onefile --name "ErowidSafeDB" --add-data "data:data" --add-data "assets:assets" gui.py
          tar -czvf ErowidSafeDB-Linux-x64.tar.gz -C dist ErowidSafeDB

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-binary
          path: ErowidSafeDB-Linux-x64.tar.gz

  build-macos:
    name: Build macOS Standalone App Bundle (.zip)
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Compile Native Cocoa Wrapper and Bundle Resources
        run: |
          mkdir -p bin
          swiftc -O -framework Cocoa -framework WebKit src_native/main.swift -o bin/ErowidSafeDB
          mkdir -p "Erowid SafeDB.app/Contents/MacOS" "Erowid SafeDB.app/Contents/Resources"
          cp bin/ErowidSafeDB "Erowid SafeDB.app/Contents/MacOS/ErowidSafeDB"
          cp gui.py "Erowid SafeDB.app/Contents/Resources/"
          cp -R erowid_safedb "Erowid SafeDB.app/Contents/Resources/"
          cp -R data "Erowid SafeDB.app/Contents/Resources/"
          cp -R assets "Erowid SafeDB.app/Contents/Resources/"
          ditto -c -k --sequesterRsrc --keepParent "Erowid SafeDB.app" ErowidSafeDB-macOS.zip

      - name: Upload macOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-binary
          path: ErowidSafeDB-macOS.zip

  publish-release:
    name: Publish Standalone GitHub Release
    needs: [build-windows, build-linux, build-macos]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download Windows Binary
        uses: actions/download-artifact@v4
        with:
          name: windows-binary

      - name: Download Linux Binary
        uses: actions/download-artifact@v4
        with:
          name: linux-binary

      - name: Download macOS Binary
        uses: actions/download-artifact@v4
        with:
          name: macos-binary

      - name: Create or Update GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ inputs.tag }}"
          if [ -z "$TAG_NAME" ]; then
            TAG_NAME="${GITHUB_REF_NAME}"
          fi
          if [ "$GITHUB_REF_TYPE" != "tag" ] || [ -z "$TAG_NAME" ]; then
            TAG_NAME="v2.5.0"
          fi

          echo "Target Release Tag: $TAG_NAME"

          cat << 'EOF' > release_notes.md
          ### 🚀 Erowid SafeDB Standalone Releases

          Pre-compiled, self-contained standalone applications for macOS, Windows, and Linux. No Python installation required!

          - 🍏 **macOS**: Download `ErowidSafeDB-macOS.zip`, unzip, and launch **Erowid SafeDB.app**.
          - 🪟 **Windows**: Download `ErowidSafeDB-Windows-x64.zip`, extract, and launch **ErowidSafeDB.exe**.
          - 🐧 **Linux**: Download `ErowidSafeDB-Linux-x64.tar.gz`, extract (`tar -xvf ErowidSafeDB-Linux-x64.tar.gz`), and run `./ErowidSafeDB`.
          EOF

          # Check if release exists; if so, upload assets to it. Otherwise create it.
          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            echo "Release $TAG_NAME exists. Uploading/overwriting standalone assets..."
            gh release upload "$TAG_NAME" ErowidSafeDB-Windows-x64.zip ErowidSafeDB-Linux-x64.tar.gz ErowidSafeDB-macOS.zip --clobber
          else
            echo "Creating new release $TAG_NAME..."
            gh release create "$TAG_NAME" ErowidSafeDB-Windows-x64.zip ErowidSafeDB-Linux-x64.tar.gz ErowidSafeDB-macOS.zip \
              --title "Erowid SafeDB Pro $TAG_NAME — Standalone Applications" \
              --notes-file release_notes.md
          fi
